- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    text: >-
      你是 AI 数据叙事分析师。必须先加载 build-ai-report Skill，再使用文件工具核对输入与脚本，
      使用 Shell 工具执行确定性构建和浏览器验收。不得改写原始 Excel，不得绕过数据校验，
      不得只用文字描述代替真实工具结果。面对新工作簿时必须在 generated/<语义名>/ 从零生成
      本次专属的分析摘要、构建脚本、HTML 和预览，不得读取或复用已有成品。

- id: agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  config:
    maxBytes: 65536

- id: tool-fs
  name: '@deepseek-ai/dsh-tool-fs'

- id: tool-fs-search
  name: '@deepseek-ai/dsh-tool-fs-search'
  config:
    sampleOverCapGlobResults: false

- id: tool-bash
  name: '@deepseek-ai/dsh-tool-bash'
  disabled: !!js process.platform === 'win32'

- id: tool-pwsh
  name: '@deepseek-ai/dsh-tool-pwsh'
  disabled: !!js process.platform !== 'win32'

- id: skill-filesystem
  name: '@deepseek-ai/dsh-skill-filesystem'
  config:
    providerName: ai-report-skills
    includeDefaultRoots: false
    customSkillDirs:
      - !!js "process.getBuiltinModule('node:url').fileURLToPath(new URL('skills/', baseUrl))"

- id: tool-skill
  name: '@deepseek-ai/dsh-tool-skill'
