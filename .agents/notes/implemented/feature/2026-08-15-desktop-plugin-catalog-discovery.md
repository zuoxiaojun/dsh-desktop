# Agent Note: Desktop plugin catalog discovery

Status: implemented

English | [中文](2026-08-15-desktop-plugin-catalog-discovery.zh.md)

## Problem

The Desktop settings surface exposed plugin configuration and Loader inventory, but it had no product-owned discovery view. A marketplace-shaped page could not safely accept package names, URLs, or arbitrary Skill folders from the renderer because discovery metadata is untrusted input and later Features will use the same exact-version identity for compatibility, installation, activation evidence, and uninstall. The product also needed a clear answer for Skills: Skill packs packaged as DSH Bundles should be discoverable without creating a second raw-Skill installation authority.

## Decision

**Catalog discovery is a Desktop-owned boundary.** `@deepseek-ai/dsh-plugin-center-contracts` defines strict exact-field decoders for catalog summaries, details, filters, compatibility, freshness, artifacts, and declared runtime identities. Desktop searches the fixed npm registry for `dsh-plugin`, the discovery keyword documented by DeepSeek Harness, then fetches exact-version metadata and retains only packages that declare `dsh.bundle`. The main process owns HTTPS origins, response and archive bounds, immutable integrity checks, atomic authority-cache replacement, offline fallback, and IPC sender checks. The preload exposes fixed methods; the renderer cannot submit a source URL, filesystem path, package command, or executable.

**Plugins and Skill packs share one catalog identity.** A catalog item carries `catalogKind: plugin | skill-pack`, one exact package version, Bundle membership, and the identities activation checks must observe. A Skill pack is an ordinary validated DSH Bundle whose declaration includes `skill` capability and, when published, `expectedSkillIds`; raw standalone Skill folders are not marketplace items and receive no separate install path.

**Discovery is a first-level application page, while existing plugin settings stay independent.** `@deepseek-ai/dsh-client-ui-plugin-center` contributes a sidebar action through `sidebar.primary.action` and an independent keyed page through `main.page`. `ui-layout` owns the selected `primaryPage`, keeps the Conversation surface mounted, and `ui-workspace` closes that page when a user starts or opens a session. The page follows the Codex reference hierarchy with Plugins/Skills navigation, centered search and content, an installed icon strip, public/personal scopes, responsive two-column unboxed discovery rows, and breadcrumb navigation to exact-version detail in the same page. A fresh network result counts as complete only when it fills the requested limit; the six-entry cold-start batch stays visible while one background refresh expands the remaining sections. An uninstalled public entry exposes a direct install action; an installed catalog entry is resolved from the current Profile projection and exposes only its backend-supported update, enable, disable, or uninstall actions through a compact overflow menu. The expanded installed section remains available for source, runtime, compatibility, configuration, and inventory details, while existing Settings paths stay independently reachable. Install and management actions appear only through handed-off bridge contracts and reuse exact-version preflight, confirmation, and durable operation state.

**The live source is the prebuilt npm DSH Bundle ecosystem, with several bounded discovery signals.** The `dsh-plugin` keyword index supplies the default catalog, while a non-empty search also performs one bounded npm text query. Those two reads enrich each other but do not fail as a unit: a successful text query can return its ranked exact match when the broad keyword index is rate-limited, and a working index can still serve a query when the bounded text endpoint is temporarily unavailable; either partial result carries the existing network-unavailable notice rather than masquerading as a complete response. An exact scoped package name, with an optional exact version, uses the fixed registry's direct packument and version metadata instead of scanning the index. Ranking treats the unscoped tail as an exact match, so `dsh-web-ui-all` can return `@linxin666/dsh-web-ui-all`, while the full scoped name returns only that package. The keyword is a community discovery signal, not DeepSeek endorsement or an installation requirement.

**Exact detail hydration retries only transient transport failures.** Reading an uncached detail revalidates exact metadata and the immutable artifact. One connection failure, 408/425/429, or 5xx response is retried once; a bounded `Retry-After` controls that delay when the server provides it. Invalid metadata, oversized responses, malformed archives, identity mismatches, and Bundle validation failures remain single-attempt failures because another identical request cannot make them valid.

**An explicit GitHub repository URL is a locator, never an artifact authority.** Desktop accepts only canonical `https://github.com/<owner>/<repo>` input, reads a bounded default-branch tree and limited-depth package manifests from fixed GitHub origins, then maps Bundle declarations back to exact packages on the fixed npm registry. It does not clone, build, run lifecycle scripts, or install repository source. A package receives authority only after its exact npm metadata and immutable tarball pass the ordinary validation path. Successful mapping, partial source-only packages, source-only repositories, missing Bundle manifests, and network failure produce distinct actionable notices instead of a false generic empty result.

**Aggregate Bundles declare complete ownership before mutation.** Tarball validation parses the patch with the Host's own YAML schema and extracts only actual inserted Loader entries, rather than approximating YAML with text matching. A module owned by the Bundle must be a top-level dependency or optional dependency at an exact version; git, file, workspace, tag, and semver-range references are rejected. A patch may also reuse a module supplied by the closed Desktop Host runtime. That allowlist is derived from the packaged Host manifest's dependency map at startup, never from an npm scope or publisher name, and a truly absent third-party module still fails validation. Persisted catalog authority is namespaced by a deterministic fingerprint of that dependency set, so an app version with a different Host closure must validate the Bundle again. Desktop reads exact package-owned dependencies' npm metadata to derive all declared client modules. Installation must observe that complete client set, and disable or uninstall treats the same set as target-owned, so removing a Bundle cannot mistake its child modules for unrelated runtime loss. Missing `engines.node` is displayed as publisher-undeclared and uses the Studio runtime guard only for technical preflight; it is never presented as a publisher promise.

**Discovery cache version 2 retains both keyword and query-discovered candidates.** The cache remains metadata-only and cannot authorize installation. Older cache documents fail closed and discovery falls back to the network or already validated authority; opening or installing any cached result re-reads exact metadata before tarball validation. Every query also caps exact-metadata hydration at 96 ranked candidates, independently of npm's reported total.

**Browser development uses the full Desktop UI composition plus an explicit fixture seam, not a second production authority.** `pnpm run dev:desktop:web` (compatibility alias `dev:plugin-center`) sets `DSH_DESKTOP=1` to enable the same Desktop client UI roster and skin, enables the same Plugin page through a Host-injected marker and deterministic read-only bridge, and isolates `DSH_HOME` under project artifacts. The page root exposes a development data marker and hover explanation without adding a banner that distorts the reference UI; URL scenarios cover normal, empty, stale-cache, and failed reads. This entry cannot start real MCP, Electron IPC, filesystem, package-manager, Host-restart, or installation actions, and Desktop always prefers the preload bridge.

## Alternatives considered

**Load a public registry directly from the React client.** Rejected because it would move network policy, cache ownership, and source selection into an untrusted renderer and make later mutations depend on client-supplied authority.

**Treat any local Skill folder as a marketplace item.** Rejected because raw Skills do not carry the exact-version package, Bundle, compatibility, integrity, and runtime-evidence contract required for safe one-click lifecycle operations.

**Replace the existing plugin settings pages with the marketplace.** Rejected because configuration cards and Loader inventory serve distinct installed-state and diagnostic journeys that discovery does not own.

**Render an install control before its backend contract is handed off.** Rejected because an affordance without F002 compatibility evidence and the F003/F005 transaction and recovery path would advertise a lifecycle the product cannot verify.

## Verification

Contract tests cover malformed catalog input, exact identity, npm artifact origins, Skill-pack requirements, cache ownership, atomic replacement, and stale fallback. Desktop tests cover fixed bridge methods, sender/origin rejection, independent keyword/text-query failure, short/full/pinned package names, explicit GitHub mapping, aggregate dependency rejection, closed-Host module reuse, Host-closure cache invalidation, one transient exact-detail retry, exact tarball hydration, offline authority reuse, install and management transactions, and recovery. Live npm acceptance resolves `@tt-a1i/archify-dsh@0.1.0`, accepts its `@deepseek-ai/dsh-skill-filesystem` reference from the real Desktop Host manifest, and produces eligible detail with the `archify-skill-filesystem` Loader entry. Live macOS acceptance also resolves `@linxin666/dsh-web-ui-all@0.1.19` by short name, full name, pinned name, and repository URL, validates its 13 Loader entries and 12 client modules, installs it in an isolated Profile, survives a full Desktop restart, and then commits uninstall with all target runtime evidence removed. Client tests retain the first-level page, search, in-place detail retry, confirmation, progress, installed management, and browser-development journeys. `pnpm run dev:desktop:web` remains the UI loop, while real package mutation requires Desktop.

## Consequences

Desktop users can search the public DSH ecosystem and install, run, update, disable, enable, or uninstall validated exact npm Bundles without gaining source-selection or package-manager authority in the renderer. Compatibility, mutation, recovery, and installed projection consume one strict identity and evidence vocabulary. The trade-off is that ecosystem packages remain community code with broad process authority; validation proves immutable identity and activation declarations, not official code review.
