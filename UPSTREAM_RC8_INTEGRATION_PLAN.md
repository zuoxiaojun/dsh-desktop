# DeepSeek Harness rc.8 上游升级与桌面兼容计划

本文件是 DeepSeek Harness Studio 从官方 Harness `rc.5` 升级到 `rc.8` 的单一中文内部执行计划。它负责升级范围、兼容策略、桌面 UI 调整、实施顺序和验收要求，不替代现有 PRD、Product Spec、Feature Spec 或插件中心测试计划，也不维护英文副本。

## 一、升级基线与目标

| 项目 | 当前状态 | 本轮目标 |
|---|---|---|
| 官方 Harness 基线 | 产品版本 `dsh 0.1.0-rc.5`；官方主线提交 `47f943859bef60e4160492346772ded9b24f765a` | `dsh-v0.1.0-rc.8`，提交 `141eb6fef83422698aef7a981029e843e8161534` |
| Studio 桌面版本 | `0.1.0-rc.11` | Harness 升级完成后再确定下一个 Studio 预览版本号 |
| 桌面平台 | macOS arm64、Windows x64 | 两个平台继续可安装、启动和卸载 |
| 本地产品能力 | 插件中心、插件发现、Preset 广场、应用中心、视觉增强、主题皮肤 | 全部保留，并接入官方新增能力 |
| 开发策略 | Desktop Web 快速验收，模块完成后验证真实 Electron | 继续沿用；全部核心功能通过后再统一生成安装包 |

代码审计表明，官方从 Studio 使用的 `rc.5` 主线基线到 `rc.8` 累计 647 个提交、1,767 个变更文件；与 Studio 同时改动的共享代码为 77 个文件。临时真实合并演练排除文档、图片和测试素材后，需人工融合的产品源码与构建配置为 18 个文件。官方仓库没有 `apps/desktop`，Studio 的 Electron、安装器、插件事务、Preset、应用中心和主题资源不会被官方文件直接替换。

## 二、成功标准

本轮升级只有同时满足以下条件才算完成：

1. Harness 核心、Web 组合和官方内置 Profile 使用 `rc.8` 行为，官方新增功能可以从 Studio 的真实组合中到达。
2. 插件中心、插件发现、Preset 广场、应用中心、FF–LLM Wiki、视觉增强和主题皮肤保持可用，不因官方 UI 重组而消失、重复或改变入口。
3. 升级不覆盖用户的 `DSH_HOME`、工作区、会话、凭据、用户插件、用户 Preset、应用数据或 `cordis.patch.yml`。
4. Desktop 启动只打开 Electron 窗口，不因官方 `dsh web` 自动打开浏览器而额外弹出外部页面。
5. 新增 UI 使用现有主题 token，在官方主题和所有 Studio 皮肤下均可读，不出现白底、透明度失控、遮挡或侧栏入口错位。
6. macOS 源码桌面验收通过后生成 macOS 包；Windows 由 GitHub Actions 构建并完成安装、Host 启动、卸载和旧目录残留恢复验证。

本轮不重写 Electron 架构，不取消赋范品牌，不把应用中心或 Preset 合并进插件中心，不恢复在线自动更新，也不把上游全部文档重新发布到 Studio 仓库。

## 三、兼容架构

升级后继续维持三层所有权：

| 层级 | 所有者 | 本轮处理方式 |
|---|---|---|
| Harness 核心层 | DeepSeek Harness 官方 | 同步 Agent、Session、LLM、工具、附件、子代理、Profile、Bundle、Web 运行时和官方修复 |
| Studio 产品扩展层 | 赋范桌面团队 | 保留插件中心、插件发现、Preset 广场、应用中心、视觉增强、主题与中文产品交互；对官方新接口做适配 |
| Desktop 平台层 | 赋范桌面团队 | 保留 Electron、preload、Host supervisor、IPC、本机文件选择、安装器、恢复和发布工作流 |

### Git 同步方式

1. 从当前 `main` 创建 `integration/upstream-dsh-rc8`，不直接在 `main` 合并官方代码。
2. 添加只读 `upstream` remote，并以官方 `rc.5` 主线提交 `47f943859bef60e4160492346772ded9b24f765a` 建立一次 Git 祖先锚点；该提交与对应 `rc.5` 发布提交拥有相同源码树，该操作只记录共同基线，不改变当前文件内容。
3. 合并官方 `dsh-v0.1.0-rc.8` Tag，让未修改的官方文件自动升级，只人工解决共享路径冲突。
4. 保留这次上游合并关系。后续 `rc.9`、`rc.10` 直接在新的集成分支合并官方 Tag，不再重新建立基线。
5. 集成分支通过全部验收后才能进入 `main`；失败时删除集成分支即可，当前稳定版本和 Release 不受影响。

### 配置与用户数据兼容

- `DSH_HOME` 路径和现有 Profile 不迁移、不清空；测试使用临时 Home，不直接改真实用户目录。
- 官方 Bundle 更新与 Studio 插件事务分开：官方默认组合随应用升级，用户插件仍以 Desktop 插件管理器的扫描结果和事务日志为权威。
- 用户 `cordis.patch.yml` 始终位于官方 Bundle 和 Studio 默认组合之后，升级不得覆盖用户层。
- 默认会话持久化继续使用 JSONL。官方新的 SQLite 会话布局不兼容旧 SQLite；默认用户不受影响，检测到用户显式启用旧 SQLite 时先备份并阻止静默打开，迁移方案通过后才允许切换。
- 凭据继续通过现有 Credentials 服务和受控设置页面写入，升级不得把 API Key 写入仓库、环境快照、诊断文件或 UI 回显。
- 官方 Profile Bundle 可以进入插件发现和安装链，但必须经过 Studio 已有的确定版本、Bundle 声明、完整性、平台、权限和恢复检查，不能绕过 Desktop 安装权威。

## 四、官方功能接入清单

| 官方更新 | Studio 接入决定 | 对现有功能的影响 | Desktop UI 调整 | 优先级 |
|---|---|---|---|---|
| 插件自行注册设置卡片 | 完整接入官方动态设置卡片 | 插件中心仍负责安装与生命周期，设置页负责插件配置 | 设置页“插件”区域展示插件注册的卡片；插件中心已有“配置”按钮跳转到对应卡片，不新增重复配置页面 | P0 |
| Codex、Claude Code 子代理接入 Job Panel | 接入官方任务状态与操作 | 不改变普通会话；只有实际产生子代理任务时显示 | 复用会话头部后台任务按钮，按需显示子代理类型、状态和耗时；不新增常驻侧栏入口 | P1 |
| MCP/ACP 持久化图片附件 | 接入官方附件存储与转发 | 增强现有图片会话、MCP 和 PTC 流程 | 延续当前附件缩略图和错误状态，不新增独立页面 | P0 |
| DeepSeek `low` 推理强度 | 接入模型能力 | 扩展现有模型选择，不改变默认 `high` | 模型或推理强度菜单新增“低”；保持可稳定点击和选中态 | P0 |
| 提问卡片折叠与草稿保留 | 接入官方交互 | 保留现有提问流程并减少长卡片占用 | 提问卡片标题区增加展开/收起按钮，折叠不清除已填草稿 | P1 |
| DeepSeek 原生图片请求 | 与 Studio 视觉增强统一成一个入口 | 避免官方原生视觉与辅助视觉同时执行或出现两个开关 | 保留现有“视觉增强”按钮并继续作为开关；开启后自动选择原生视觉或已配置的兼容视觉提供方，并显示本次实际路径 | P0 |
| `/goal`、`/plan` 接收图文输入 | 接入官方命令附件信封 | 复用统一附件和视觉路由 | 不新增命令按钮；用户携带图片调用命令时显示已绑定附件 chip 和实际视觉模式 | P0 |
| `@` 菜单引用文件和会话 | 完整接入 | 扩展输入框，不改变现有工作区和会话列表 | `@` 菜单增加“文件”和“会话”结果组；选中后在输入框中显示可删除引用 chip | P0 |
| Codex、Claude Code 作为 Profile Bundle 安装 | 接入现有插件发现与安装验证 | 不把 Bundle 伪装成 Preset，不新增第二套安装器 | 插件发现增加“Agent 与工作流”筛选；详情展示提供方、权限模式和实例信息；安装后仍在插件中心启停和卸载 | P1 |
| Codex 非交互权限与命名实例 | 接入 Bundle 配置卡片 | 复用插件设置和现有权限语义 | 对应插件设置卡片提供权限模式和实例名称，不在主界面新增全局开关 | P1 |
| Windows 持久 PowerShell PTY | 完整接入 | 改善 Windows 工具运行，不影响 macOS | 无新增导航；终端/工具卡片显示 PowerShell 会话状态，Windows Minimal Preset 使用官方默认能力 | P0 |
| 模型选择器、侧栏搜索、工作流面板修复 | 合并官方修复并保留 Studio 样式 | 直接解决选择不稳定、焦点和窄屏问题 | 不新增功能入口；调整菜单层级、焦点、选中态和窄屏布局 | P0 |
| 本地文件打开失败可重试 | 接入官方失败状态 | 改善文件结果卡片 | 文件打开失败提示增加“重试”，重试仍失败时保留可复制路径和明确原因 | P1 |
| 并发 `web_search`、子代理报告唤醒父任务、历史分叉性能 | 完整接入运行时 | 提升现有会话效率 | 不新增按钮，仅更新状态与结果展示 | P1 |
| 客户端品牌插槽 | 迁移 Studio 品牌到官方插槽 | 减少对官方组件的硬编码修改 | 视觉外观保持现状；Logo、标题和空状态通过插槽注入，官方升级不再覆盖赋范品牌 | P0 |
| `dsh web` 自动打开浏览器 | Web 源码运行保留，Desktop 启动显式关闭 | 防止启动桌面端时同时弹出浏览器 | Desktop Host 启动参数增加 `--no-open`，无新增 UI | P0 |
| SQLite 存储压缩布局 | 默认不切换；保留为可选能力 | 默认 JSONL 会话不迁移；显式 SQLite 用户需要兼容提示 | 默认无按钮；检测到不兼容数据库时显示可恢复错误，不自动删除或覆盖 | P1 |

## 五、桌面 UI 变化总表

### 左侧导航

左侧一级入口保持现有产品顺序和语义，不因为官方 rc.8 新功能增加新的一级页面：

1. 新会话
2. 插件中心
3. 插件发现
4. Preset 广场
5. 应用中心
6. FF–LLM Wiki 快捷入口，仅在用户开启“显示在侧边栏”后出现，默认关闭
7. 工作区与会话列表
8. 设置

官方子代理、附件、Job Panel 和文件引用属于会话或设置内能力，不占用新的侧栏按钮。官方品牌插槽只改变注入方式，不改变用户看到的 Studio Logo、产品名称和赋范官方标识。

### 对话输入区

| 位置 | 当前状态 | 计划变化 |
|---|---|---|
| 视觉增强按钮 | 辅助视觉开关 | 保留名称和开关交互；开启后自动路由，按钮旁显示 `原生` 或 `兼容 · 提供方`，关闭后图片不进入模型可见上下文 |
| `@` 引用菜单 | 现有引用能力有限 | 增加文件与会话分组、键盘选择、加载/空/失败状态和已选引用 chip |
| 图片附件 | 现有图片附件和 Qwen 观察 | 接入官方原生图片信封、尺寸与累计载荷限制；失败指出是模型不支持、图片超限还是增强服务未配置 |
| `/goal`、`/plan` | 主要接收文本 | 允许携带当前图片与引用，不额外增加按钮 |
| 提问卡片 | 展开显示 | 增加折叠按钮并保留草稿 |

视觉增强的兼容规则如下：

- 开启后，当前模型明确支持官方原生图片请求时使用原生视觉；模型不支持图片时，使用已经成功配置的阿里云百炼或 OpenRouter 兼容视觉。
- 原生视觉与兼容视觉互斥，同一张图片只进入一条链路，不能先转成文字后再重复发送原图。
- 两条链路都不可用时阻止发送并给出配置入口，不静默丢弃图片，也不把 UI 可见误报成模型可见。
- 按钮和发送前提示显示本次实际使用的路径及图片接收方；设置页只保留兼容提供方配置，路由固定由“视觉增强”这个开关自动选择，不增加第二个模式开关。
- 关闭后图片不会进入模型可见上下文，普通文件引用仍按官方文件引用规则处理。

### 模型与设置

| 页面 | 新增或调整 |
|---|---|
| 模型设置 | DeepSeek 提供方增加“允许原生图片请求”；模型能力明确显示是否支持图片；视觉设置保留兼容提供方配置，不增加与“视觉增强”重复的路由开关；推理强度增加“低” |
| 插件设置 | 渲染插件自行注册的动态设置卡片；已有插件中心“配置”按钮定位到对应卡片 |
| Agent Preset | 保持现有选择和管理；Profile Bundle 不混入 Preset 列表 |
| 背景 | 官方新增组件全部绑定 Studio 主题 token，轨迹、设置、弹层和菜单继续匹配当前皮肤 |
| 软件更新 | 继续显示“暂未开放在线更新”，不因上游升级重新启用 |

### 插件中心与插件发现

- 插件发现新增“Agent 与工作流”能力筛选，但不增加新的插件市场页面。
- 官方 Codex/Claude Code Bundle 使用现有搜索、详情、风险说明、安装、启停和卸载闭环。
- 详情页增加 Profile Bundle 类型、提供方、权限模式、命名实例和所需外部程序说明。
- 安装完成后仍停留当前页面；需要配置时提供“去配置”，不自动切换首页或创建会话。
- 官方动态插件面板不会替代 Studio 插件中心；它只提供 Cordis 运行配置和诊断能力。

### 会话头部与结果卡片

- 后台任务按钮只在当前会话存在任务时显示，不让普通会话永久多一个按钮。
- 子代理任务行展示提供方、实例名、状态、耗时和结果；停用或卸载提供方后，历史记录仍可阅读。
- 文件打开失败的结果卡片增加“重试”；路径无效时继续提供复制路径，不无限重试。
- 持久化图片在重启后仍能显示；模型不可读取时不能把“UI 可见”误报成“已进入模型”。

## 六、现有 Studio 功能保护清单

| 现有功能 | 升级风险 | 兼容措施 | 必验结果 |
|---|---|---|---|
| 插件中心安装、启停、更新、卸载、恢复 | 官方 Profile/Bundle 与设置接口变化 | 保留 Desktop 安装权威，映射官方新 Bundle 类型和动态设置卡片 | 安装/卸载不跳页、不整屏闪烁，重启状态真实，卸载后可重新安装 |
| 插件发现与目录搜索 | 官方包和目录元数据增加 | 严格解码新增字段，未知字段不破坏旧目录；现有精选、热门、最新和搜索保留 | 首次进入不空白，后台补齐目录，搜索覆盖公开插件 |
| Preset 广场 | 官方 Agent Preset 与 Profile Bundle 容易混淆 | Agent Preset 继续由 AgentPresets 权威管理；Profile Bundle 进入插件中心 | 六套赋范官方 Preset、社区列表、安装、删除和用于新会话全部可用 |
| 应用中心与 FF–LLM Wiki | 官方 UI 插槽和侧栏变化 | 保留独立一级应用中心；快捷入口状态继续由同一持久设置控制 | 默认隐藏 Wiki 快捷入口，开启/关闭立即同步左侧，应用可正常启动 |
| 视觉增强 | 官方原生视觉与现有辅助视觉链路重叠 | 保留“视觉增强”单一开关并自动选择一条视觉链路，禁止同一张图重复处理 | 开关状态和实际路径明确，缺少配置可恢复，历史图片可追溯 |
| 主题和背景 | 官方布局、品牌和组件新增 | 将赋范品牌迁移到官方品牌插槽；新组件使用现有主题 token | 官方原版及全部内置皮肤下，对话、轨迹、设置、插件、Preset、应用页面一致可读 |
| 模型选择和凭据 | 官方模型接口与选择器变化 | 合并官方选择修复，保留现有凭据写入、环境变量只读提示和 OpenRouter 支持 | 菜单稳定选中、可切回模型、Key 不回显、不被只读环境变量静默遮蔽 |
| 工作区和目录选择 | 官方文件引用与当前原生选择器相交 | 原生目录选择继续由 Desktop bridge 持有；文件引用只读取已授权工作区 | macOS/Windows 选择目录不闪退，引用不能越过工作区授权 |
| Windows 安装残留恢复 | 官方运行时和依赖体积变化 | 保留 NSIS 进程结束、残留目录检查和自动恢复逻辑 | 旧进程、旧目录和缺失卸载器场景可重新安装 |
| 软件更新页面 | 官方版本号变化 | 继续禁用在线更新，只显示当前 Studio 版本和手动下载说明 | 不误报“已是最新版”，不发起不存在的更新下载 |

## 七、实施阶段

### 阶段 0：冻结与保护基线

- 记录当前 `main`、macOS/Windows `rc.11` Release、上游 `rc.5` 主线基线 `47f943859bef60e4160492346772ded9b24f765a` 和目标 `rc.8` Tag。
- 使用临时 `DSH_HOME` 复制一份不含凭据的会话、插件、Preset 和应用状态作为回归样本。
- 建立 `integration/upstream-dsh-rc8`，确认 `main` 保持干净。

通过条件：能够从当前 `main` 重现现有 Web 与 Desktop 核心旅程；集成失败可以不改 `main` 直接回退。

### 阶段 1：建立长期上游同步关系

- 添加官方 remote 和 `rc.5` 祖先锚点。
- 合并 `dsh-v0.1.0-rc.8`。
- 对上游删除的文档、官方新增文档和 Studio 单一中文内部文档分别处理，不让文档冲突干扰产品代码。
- 先解决 workspace、TypeScript、构建脚本和 lockfile，再处理业务代码。

通过条件：`pnpm install --frozen-lockfile`、Host 类型构建和客户端类型构建通过；不要求此时 UI 完整。

### 阶段 2：升级 Harness 核心和运行能力

- 接入官方 LLM、附件、命令、文件/会话引用、子代理、Job、PowerShell、Web Search 和会话修复。
- 保持默认 JSONL 会话持久化；对 SQLite 不兼容状态增加明确保护。
- 将官方 Profile Bundle 纳入 Studio 插件兼容检查。

通过条件：核心聚焦测试、Host API 测试、附件/会话回放、子代理/Job 和 Windows PowerShell 合同测试通过。

### 阶段 3：融合共享 Web UI

- 解决设置、模型选择、侧栏、工作区、对话输入和 `apiproxy` 的共享冲突。
- 接入动态设置卡片、`@` 文件/会话引用、提问卡片折叠、模型选择修复和官方窄屏布局。
- 将赋范 Logo、标题和空状态迁移到官方品牌插槽。

通过条件：`pnpm run dev:desktop:web` 可访问；现有一级入口、设置、会话和新增官方交互均可在浏览器完成验收。

### 阶段 4：恢复 Studio 产品扩展

- 重新挂载插件中心、插件发现、Preset 广场、应用中心、FF–LLM Wiki、主题和统一视觉增强路由。
- 适配官方新增 Bundle、设置卡片和附件能力，但不改变各自已安装权威。
- 验证安装、卸载或应用侧栏开关后只局部更新，不导航回首页、不整屏闪烁。

通过条件：现有 Studio 核心旅程全部通过，新增功能从真实 Studio 组合中可到达。

### 阶段 5：真实 Electron 验收

- Desktop Host 启动增加 `--no-open`，确认不会额外打开浏览器。
- 验证 preload、IPC、目录选择、Host 重启、插件恢复、应用启动和主题持久化。
- macOS 先执行源码桌面验证；Windows 专属 PowerShell、路径和进程行为由自动化合同及最终 Actions 验收。

通过条件：macOS Electron 启动并完成核心旅程；没有访问真实用户数据、遗留测试进程或额外浏览器窗口。

### 阶段 6：统一打包与发布

- 所有 Web 和源码 Desktop 验收完成后，统一生成 macOS 包。
- macOS 包通过后建立新的预发布 Release。
- GitHub Actions 构建 Windows x64，并完成真实安装、Host 启动、卸载、残留恢复和 Release 上传。
- README 下载链接只在两个平台资产均通过后更新。

开发阶段不反复生成安装包；安装器、升级、卸载和平台签名只在本阶段验证。

## 八、验收旅程

| 旅程 | Web 开发验收 | macOS Electron | Windows 安装包 |
|---|---|---|---|
| 启动与首页 | UI 组合启动 | 只弹 Electron，不弹浏览器 | 安装后正常启动 |
| 现有会话与工作区 | 浏览、切换、创建 | 原生目录选择、重启保留 | Windows 目录选择和路径 |
| 插件中心 | 浏览器数据和 UI 状态 | 真实安装、启停、卸载、恢复 | npm、进程和残留目录 |
| 插件发现 | 目录、搜索、详情 | 真实目录和跳转管理 | 与 macOS 同等目录能力 |
| Preset 广场 | 三个 Tab、详情、状态 | 真实安装、删除、用于新会话 | 同一能力包与路径安全 |
| 应用中心 | 显示/隐藏和状态 | FF–LLM Wiki 真实启动 | Windows 应用启动 |
| 视觉增强 | 单一开关、自动路由和实际路径提示 | 原生/兼容视觉真实附件旅程 | 附件路径与载荷限制 |
| `@` 文件与会话引用 | 搜索、选择、删除 chip | 真实工作区文件与历史会话 | Windows 路径与权限 |
| 动态插件设置卡片 | 注册、跳转、失败状态 | 真实配置与重启保留 | 与插件安装器联动 |
| 子代理与 Job Panel | 状态与按需入口 | Codex/Claude Bundle 和任务状态 | Windows 进程与 PowerShell |
| 模型与凭据 | 选择、低推理、原生视觉开关 | Key 写入与模型切换 | 环境变量只读行为 |
| 主题与布局 | 全页面主题检查 | 原版及内置皮肤 | Windows 窗口密度与圆角 |

## 九、停止与回退条件

出现以下任一情况时停止合并，不把集成分支推入 `main`：

- 插件、Preset 或应用的已安装权威发生混用。
- 升级需要删除、覆盖或静默迁移真实用户数据。
- 新 UI 只能通过移除现有一级入口才能运行。
- Desktop 无法阻止官方 Web 自动打开外部浏览器。
- 视觉原生链路与 Qwen 链路重复发送同一图片，或 UI 无法说明实际使用的模式。
- macOS 或 Windows 安装、卸载、重装恢复出现回归。

回退时保留 `main` 和已发布的 `rc.11`，删除未完成的集成分支及临时测试 Home。已经形成且相互独立的官方 bug 修复可以在重新评估后单独移植，但不能用零散补丁冒充完成 `rc.8` 升级。

## 十、每阶段汇报格式

每完成一个阶段，按以下内容向用户汇报：

- 完成了哪些官方能力。
- 哪些 Studio 现有功能被触及，采用了什么兼容处理。
- Desktop Web 可以从哪里进入、点击什么、看到什么。
- 桌面 UI 新增或调整了哪些按钮、菜单、状态和默认值。
- 实际运行了哪些自动化检查。
- 仍需 macOS Electron 或 Windows 安装包验证的内容。
- 是否满足进入下一阶段的条件。

## 十一、当前执行状态

| 阶段 | 状态 |
|---|---|
| 代码差异与临时合并审计 | `completed` |
| 本计划确认 | `approved` |
| 阶段 0：冻结与保护基线 | `completed` |
| 阶段 1：建立上游同步关系 | `completed` |
| 阶段 2：升级 Harness 核心 | `completed` |
| 阶段 3：融合共享 Web UI | `completed` |
| 阶段 4：恢复 Studio 产品扩展 | `completed` |
| 阶段 5：真实 Electron 验收 | `completed` |
| 阶段 6：统一打包与发布 | `in-progress` |
